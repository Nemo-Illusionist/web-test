import React from 'react';
import classes from './TLoader.module.css'

const TLoader = () => {
    return (
        <div className={classes.tLoader}>

        </div>
    );
};

export default TLoader;